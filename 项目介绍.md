# 项目介绍：陆星竹的个人 3D 作品集

> 给后来者的快速上手文档。模板本身的完整说明见根目录 [README.md](README.md)（原作者文档）。

## 这是什么

陆星竹（AI 产品经理方向）的个人作品集网站，基于开源模板 [sen-3d-resume](https://github.com/dayinji/sen-3d-resume) 改造：

- 固定 3D 背景：人物模型随页面滚动运镜（相机动画烘焙在 `me.glb` 里，滚动只是"刮"动画时间轴）
- 可滚动内容层：首屏 About → 履历时间轴（5 条）→ 作品集横向画廊（3 个项目，各 3 张截图 + 详情页）
- 纯前端 SPA：无后端、无数据库、无 API key

**线上地址**：https://personal-website.applebanana202210.workers.dev/
**仓库**：https://github.com/LYL-PP/personal-website

## 技术栈与运行

React 18 + TypeScript + Vite 5 + React Three Fiber（three.js）+ framer-motion。

```bash
cd web
npm install        # 依赖安装（慢的话加 --registry=https://registry.npmmirror.com）
npm run dev        # 本地预览 http://localhost:5173
npm run build      # 类型检查 + 构建，产物在 web/dist/
```

## 部署（改内容 = 上线）

- 部署在 **Cloudflare Workers**（静态资源），由 GitHub 集成驱动：**push 到 `main` 分支 → 自动重新构建上线**（约 1-2 分钟）
- Cloudflare 构建配置：Root directory `web`，Build command `npm run build`，Deploy command `npx wrangler deploy`
- `web/wrangler.jsonc` 是关键配置：告诉 wrangler 静态资源在 `./dist`。**不要删**，删了线上会白屏（挂的是源码而非构建产物）
- 刷新不生效时先 Ctrl+F5 强刷（浏览器缓存）

## 目录结构（web/ 下）

```
src/
  App.tsx              首屏 About 文案（COPY）、四角标、联系图标、3D Canvas 挂载
  ui/Resume.tsx        履历时间轴数据（RESUME，5 条）
  ui/Works.tsx         作品区渲染（画廊/详情弹窗/截图灯箱），一般只改样式不动逻辑
  data/works.ts        作品板块与作品列表（WORKS）、三联图位（SECTION_SHOTS）
  data/focusPoints.ts  相机停靠点名单（FOCUS_POINTS）——履历条数的唯一真源
  data/workDocs.ts     作品详情 md 的加载与 frontmatter 解析（标题支持 \n 分行）
  content/works/*.md   作品详情正文（frontmatter + markdown）
  scene/               3D 场景代码。⚠️ 原则不动；灯光/景深参数在文件顶部常量
public/
  models/me.glb        人物模型（intro3d 导出，含 CameraAction + focus-0~5 锚点）
  works/covers/        项目图（三联图位 + 详情页 banner 共用）
  textures/env.hdr     环境光照贴图
```

## 常见修改方式

| 要改什么 | 改哪里 |
| --- | --- |
| 首屏文案 / 角标 / 联系图标 | `src/App.tsx`（COPY、hero-meta、about-contact） |
| 履历内容 | `src/ui/Resume.tsx` 的 RESUME |
| **增删履历条数** | Resume.tsx + `src/data/focusPoints.ts` 的 FOCUS_POINTS **必须同步**（一一对应），glb 里也要有对应数量的 focus 锚点 |
| 板块名 / 作品名 / 标签 / 链接 / 底部简介 | `src/data/works.ts` |
| 项目图（每个 3 张） | 图放 `public/works/covers/`，在 works.ts 的 SECTION_SHOTS 登记 |
| 作品详情正文 | `src/content/works/<slug>.md`；frontmatter 的 `link` 生成"访问作品"按钮，`banner` 为顶部横幅（建议 3:1 宽图） |
| 换人物模型 | 用 intro3d.com 摆模型和机位后导出 me.glb 覆盖 `public/models/me.glb`；相机动画必须烘在 glb 里（裸模型没有运镜） |

## 样式速查（src/styles.css）

- 履历条目间距：`.tl-entry { padding-bottom }`（当前 15vh）
- 履历末尾到作品区距离：`.tl-entry:last-child` + `.resume` 底部 padding
- 作品区首卡入场留白：`.wk-track { padding-left }`（当前 50vw）
- 作品区对角入场幅度：`src/ui/Works.tsx` 里 `enterX/enterY`（当前 55vw/65vh）
- 时间轴悬浮放大：`.tl-entry:hover { transform: scale(1.15) }`

## 注意事项

- 原模板作者的个人内容（姓名/肖像模型/品牌 logo/社交链接）不在 MIT 许可内（见仓库 NOTICE），已全部清除替换；不要从模板历史里恢复它们
- 字体走 Google Fonts + jsDelivr（霞鹜文楷）；若国内加载失败，需下载到 `public/fonts/` 自托管并在 styles.css 加 @font-face
- 图片都在 git 里（covers 目录不受 .gitignore 限制），注意单图尽量 < 1MB
